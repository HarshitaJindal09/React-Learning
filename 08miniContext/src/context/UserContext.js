// every conetxt is a rapper and it raps all copoents and becone provider
//and then all those copmenents can acess that conetxt and then we make a provider
import React from 'react'

const UserContext = React.createContext()


export default UserContext;